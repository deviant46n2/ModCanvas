---
description: Lists every owed item from the profile — pending explain-backs, unconfirmed "what we built" lines, past-due re-reviews. INFORMATIONAL ONLY: never blocks, never forces. Each item is schedule (student picks when) or carry (stays visible). The student's veto on forced explain-backs is absolute (2026-08-09).
agent: tutor
---

Owed items: $ARGUMENTS

List the ledger at session start and on request:

1. **Pending explain-backs** — declined or carried; each with what it covers
   and how many questions it is.
2. **Unconfirmed "what we built" lines** — the /handoff one-liners the student
   never corrected or confirmed; each is a candidate for the s21-style reset
   if too many accumulate (the cost of carrying, visible).
3. **Past-due re-review dates** — the spaced-repetition schedule.

Rules:
- **Never block, never force.** Exit clean even if everything is carried.
  Tired + in flow = carry, with zero friction.
- **Schedule or carry.** The student picks when an item happens (fresh, arc
  close) or lets it move forward. A scheduled item gets a date.
- An explain-back happens ONLY at the student's invitation ("explain that to
  me") or their scheduled time. No session may open with a demand.
- The ledger is data for conversation, never a gate.
