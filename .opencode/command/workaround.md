---
description: Consults and maintains docs/workarounds.md — the lived-experience register (s21 cont.4 lesson: real workarounds must be surfaced, never withheld). Use at diagnostic start and whenever a workaround surfaces.
agent: tutor
---

Workaround register: $ARGUMENTS

1. At the START of any diagnostic, read `docs/workarounds.md` and check the
   register against the current symptom BEFORE proposing theories — a known
   workaround (#1: restart the app to refresh stale textures) beats a new
   hypothesis.
2. **Ask the student for their lived-experience workarounds** — they have real
   ones we don't (the s21 cont.4 withheld workaround is the canonical case).
   Their lived experience is ground truth, not a competing hypothesis.
3. When a new workaround surfaces: add a row to `docs/workarounds.md` in the
   same pass — workaround | when | why it works | discovered date. Never let
   one die with a session.
4. When a workaround gets fixed in code: mark it resolved with the commit hash
   (kept as history, not deleted).
5. Record the durable ones as `code:gotcha` memories too (with verification
   pointers).
