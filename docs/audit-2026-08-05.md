# Codebase Standards Audit — 2026-08-05

> Full audit of the ModCanvas codebase against its own standards
> (`AGENTS.md`, `docs/PROJECT_BIBLE.md`, `CONTRIBUTING.md`), run on branch
> `fix_debt` after the `recipe` → `master` merge.
>
> Baselines at audit time: **533 frontend tests green, 258 Rust tests green.**

## Summary

| Area | Verdict |
|---|---|
| Atomic tmp+rename writes | **PASS** (central `atomic_write` primitive) |
| Windows EBUSY retry | **PASS** (retry loop) / **FAIL** (two-tier IPC pipeline absent) |
| Comment preservation | **PARTIAL** — SNBT quests (sidecar) + KubeJS PASS; live export paths + book files + TOML lose comments |
| Path security | **PASS** with gaps (unvalidated export dir, double-nested write) |
| 300-line file limit | **FAIL** — 35 Rust + 12 TS files over limit |
| 3-layer separation | **FAIL** — `core/theme/asset-cache.ts` impure; `/src/drivers/` does not exist |
| No game assets in bundle | **PASS** |
| Lazy texture materialization | **PARTIAL** — core index PASS; legacy data-URL scan still live |
| Adapter matrix | **PASS** (`dataComponents` flag unused in prod — half-realized) |
| SNBT/export fidelity | **PASS** — proptest (s23, `snbt_roundtrip.rs`) found f32 `-0.0`/integral ≥ 2^63 emission and `needs_quoting` key holes, all parked; **fixed s30** (`.0f` integral emission, quoting alignment); nested-compound comment indent remains parked in the module header |
| Offline-first / no AI deps | **PASS** |
| Docs sync | **STALE** — `README.md`, `featureparity.md`, `recipe-editor.md` |

---

## High severity

### H1. 300-line file limit pervasively violated

Standard (`AGENTS.md`): *"Single files MUST NOT exceed 300 lines of code."*

Rust (35 files over): `import.rs` 2536, `commands/modpack.rs` 1288, `quest/types.rs`
1241, `export.rs` 1073, `minecraft.rs` 1026, `config_parser.rs` 1015, `imports/snbt.rs`
916, `indexer.rs` 909, `progression.rs` 876, `ingest.rs` 820, `recipes/mod.rs` 725,
`scriptgen/kubejs.rs` 705, `quest_config.rs` 692, `path_safety.rs` 664,
`quest/analysis.rs` 621, `progression_config.rs` 611, `scriptgen/crafttweaker.rs` 595,
`imports/ftb_quests/mod.rs` 595, `mod_intelligence/modrinth.rs` 585, `db.rs` 531,
`imports/curseforge.rs` 411, `imports/packwiz.rs` 401, `ws_ipc.rs` 371, `icons.rs` 352,
`commands/progression.rs` 342, `recipes/kubejs.rs` 340, `recipe_disable.rs` 337,
`instance_textures.rs` 332, `commands/mod.rs` 329, `shared.rs` 321,
`recipes/crafttweaker.rs` 321, `imports/resolution.rs` 308, `imports/mod.rs` 307,
`instance_textures/models/tests/resolve.rs` 305.

TS (12 files over): `QuestCanvas.tsx` 1139, `QuestBookEditor.tsx` 1110,
`useAppState.ts` 485, `RecipeEditor.tsx` 441, `RecipeExplorer.tsx` 412,
`core/recipe/recipe-store.ts` 399, `services/texture-loader.ts` 389,
`services/quest-types.ts` 360, `core/recipe/recipe-editor.test.ts` 357,
`core/pack-health/checks/quests.ts` 324, `ProgressionGraph.tsx` 322,
`common/config-editor.tsx` 309.

### H2. Comment preservation breaks on live export paths

The SNBT sidecar mechanism (import → export round-trip) works, but both live export
entry points pass an **empty sidecar**, silently dropping all user comments:

- `commands/modpack.rs:1249` — `export_ftb_quests_to_dir` (called by
  `useQuestToolbarActions.ts:135`).
- `commands/progression.rs:337` — `write_quest_graph_to_instance`.

`data.snbt`, `chapter_groups.snbt`, and `reward_tables/*.snbt` are always rebuilt
from the model without sidecar merge (`export.rs:146,278,322`).

### H3. Legacy data-URL texture scan still live → RESOLVED 2026-08-09 (see item 2)

`icons.rs::scan_jar_for_textures` (icons.rs:96-99, indexer.rs:149-153) reads PNG
bytes during a scan and base64-encodes them to data URLs, persisting them to
`~/.cache/modcanvas/textures_<hash>.json` (icons.rs:165-213). AGENTS.md bans this
format ("Do not add new versions of the old data-URL index format"). Still called as
the no-instance fallback (`QuestBookEditor.tsx:401`,
`useQuestToolbarActions.ts:177,238`, `book-settings.tsx`).

### H4. Windows two-tier write pipeline absent

Standard (`AGENTS.md`): *"Windows writes MUST account for JVM file locks (EBUSY).
Always use a two-tier pipeline (Local IPC Socket bridge to the in-game mod -> Staged
.tmp disk sync fallback)."* The EBUSY retry loop exists and is correct
(`path_safety.rs:160-174`), but the IPC-socket-first pipeline and any
`#[cfg(windows)]` write path are absent.

---

## Medium severity

### M1. `core/theme/asset-cache.ts` breaks the pure core layer

Uses `localStorage` at :173/:179 and is a stateful singleton/observer
(`AssetCacheManager` with listeners, `globalAssetCache` export) — browser-global
side effects in the layer that must be environment-free.

### M2. `/src/drivers/` does not exist

`services/` fills the I/O-driver role (100% of `invoke()` calls), but the documented
`drivers/` directory is absent. Components import `services/*` directly, bypassing
the hook layer: `QuestBookEditor.tsx:3`, `App.tsx:12`, `RecipeEditor.tsx:15`,
`QuestCanvas.tsx:19`.

### M3. Path bugs on write paths

- `write_quest_graph_to_instance` double-nests: passes `…/config/ftbquests/quests`
  as `output_dir` while `export.rs:94` re-appends `config/ftbquests/quests`
  (`progression.rs:331`). Dormant (command not wired to UI) but wrong.
- `export_ftb_quests_to_dir` writes to an arbitrary user-supplied dir with no
  `validate_under_root` scoping (`commands/modpack.rs:1242-1251`).

### M4. TOML structured save loses comments

`save_structured_config` → `config_value_to_string` rebuilds the document from a
fresh `DocumentMut::new()` (`config_parser.rs:416-426`). Only per-value suffix decor
comments survive (`config_parser.rs:436-496`). Table headers, block comments, and
layout are lost.

### M5. `getSNBTSpec().dataComponents` dead in production

Defined on the interface and tested (`matrix.test.ts:99-131`) but never queried at
runtime. The "query Data Component support at runtime rather than hardcoding version
strings" intent is only half-realized — no hardcoded version strings, but no
adapter-driven gate either.

### M6. SNBT integral floats emit `1f` not `1.0f`

`snbt.rs:167` — `Float` emits `format!("{}f", v)`. Valid FTB SNBT and suffix/type
preserved, but not byte-identical to FTB output (affects `empty_weight` etc.).

---

## Low severity

- Direct (non-atomic) writes in new-file contexts: `minecraft.rs:566` (companion
  deploy), `curseforge.rs:141` / `imports/mod.rs:291` (zip extraction). Low risk.
- Test coverage gaps: `core/sync/config.ts`, `core/pack-health/pack-health-store.ts`
  untested.
- `resolve_loader_version` is live-but-dormant network code (never called by the
  frontend); mod install is user-gated, not flag-gated.

---

## Passing areas

- **Atomic write primitive** — `path_safety.rs:138-177` tmp+rename, EBUSY retry
  loop at :160-174.
- **Adapter matrix** — directory layout, per-file isolation, 3-tier fallback
  resolution, `matrix.test.ts` coverage all pass.
- **Smart-filter DSL + icon_scale** — nested `ftbfiltersystem:filter` components +
  icon_scale written in both flat and subdirs layouts.
- **Quoted-colon SNBT keys** — `needs_quoting` returns true on `:`.
- **Number suffixes** — full typed model (Byte/Short/Int/Long/Float/Double)
  round-trips. f32 `-0.0` and integral ≥ 2^63, and the `needs_quoting` key
  classes (digit-first, `+`/`/`-in-body), were parked holes at audit time
  (s23 proptest) and **fixed s30** (serializer emits `.0f` for integral
  floats; `needs_quoting` quotes anything the tokenizer can't read unquoted);
  the nested-compound comment indent remains parked. See
  `src-tauri/src/imports/snbt_roundtrip.rs` header.
- **No game assets in bundle** — `frontend/public/` only self-authored icons; no
  `bundle.resources`; banned `theme/ftbquests/` removed.
- **Lazy materialization core** — `CACHE_VERSION=8`, descriptor-only disk cache,
  `resolve_texture_urls` batches per jar (BATCH_SIZE 500), skips `bake:` keys,
  `TAG_INDEX_MEMO` separate.
- **Offline-first** — no AI/LLM/telemetry SDKs; no network on any core path;
  WebSocket is the localhost companion bridge only.

---

## Resolution plan

Follow-up work items (tracked on branch `fix_debt`):

1. Wire the SNBT sidecar through `export_ftb_quests_to_dir` and
   `write_quest_graph_to_instance`; extend sidecar merge to book-level files. ✅ DONE
2. Remove/replace the legacy data-URL scan (`icons.rs`/`indexer.rs`) with the lazy
   `instance_textures` path. ✅ DONE (2026-08-09, s29) — split decision:
   (a) `icons.rs::scan_jar_for_textures` + the whole "scan mods directory" family
   (scan, base64 `textures_<hash>.json` cache, `scan_mod_jar_textures` /
   `reindex_textures` / `get_texture_by_id` commands, browse-mods-dir UI) was
   DELETED — the materializer is instance-scoped, so a descriptor-based conversion
   would have meant building a second materializer for the no-instance fallback;
   the feature predates instance support and was dead weight. (b) `indexer.rs`
   (`scan_jar_for_items_and_textures`) was CONVERTED to enumeration-only
   `jar:<abs>!<zip>` descriptors in `texture_data_url`; item-cache version bumped
   to 3; frontend URL consumers (`ItemPickerModal`, `buildRegistryUrlMap`) now only
   treat the field as a URL when `isUsableTextureValue` passes; the s26 flat-icon
   guard keeps working on descriptor presence (regression-pinned by
   `test_scan_emits_descriptors_not_data_urls` + e2e descriptor assertions).
3. Fix the double-nested write path. ✅ DONE
4. Purify `core/theme/asset-cache.ts`. ✅ DONE
5. Preserve TOML table headers/comments on structured save. ✅ DONE
6. Split the worst 300-line offenders (top ~10 per language). ✅ DONE — the top
   tier (frontend + `import.rs`, `modpack.rs`, `quest/types.rs`, `export.rs`,
   `minecraft.rs`, `config_parser.rs`, `indexer.rs`, `snbt_sidecar.rs`) is under
   300 lines. Next-tier files remain >300 (see split-status table above).
7. Update stale docs (`README.md`, `featureparity.md`, `recipe-editor.md`). Pending.

## 300-line split status (2026-08-05)

Split to under 300 lines (structural moves only, all tests green):

- Frontend: `QuestCanvas.tsx` (1139→295), `QuestBookEditor.tsx` (1110→224),
  `useAppState.ts` (485→254), `RecipeEditor.tsx` (441→298),
  `RecipeExplorer.tsx` (412→259).
- Rust: `config_parser.rs` (1015→6 files), `minecraft.rs` (1026→8 files),
  `commands/modpack.rs` (1288→8 files), `indexer.rs` (909→7 files),
  `quest/types.rs` (1241→6 files), `imports/ftb_quests/export.rs` (1093→6 files),
  `imports/ftb_quests/snbt_sidecar.rs` (426→3 files),
  `imports/ftb_quests/import.rs` (2536→14 files: `import/mod.rs` + `chapter.rs`,
  `chapter_json5.rs`, `edges.rs`, `global.rs`, `helpers.rs`, `json5.rs`,
  `layout.rs`, `quest.rs`, `reward.rs`, `reward_tables.rs`, `standalone.rs`,
  `task.rs`, `tests.rs`).
- Rust (s29): `progression.rs` (876→hub 270 + `progression/categories.rs` 150,
  `progression/category_map.rs` 179, `progression/generation.rs` 291 — 6560e43,
  pure structural extraction, zero call-site edits, doc-less by judgment).

**Remaining known debt (still >300 lines):** `imports/ftb_quests/export_tests.rs`
(1239, test file), `imports/snbt.rs` (916), `ingest.rs`
(820), `recipes/mod.rs` (725), `scriptgen/kubejs.rs` (705), `quest_config.rs`
(692), `path_safety.rs` (664), `quest/analysis.rs` (621),
`progression_config.rs` (611), `scriptgen/crafttweaker.rs` (595),
`imports/ftb_quests/mod.rs` (595), `mod_intelligence/modrinth.rs` (585),
`db.rs` (531), `icons.rs` (352), `ws_ipc.rs` (371), `packwiz.rs` (401),
`curseforge.rs` (411), `commands/progression.rs` (343); and frontend:
`core/recipe/recipe-store.ts` (399), `services/texture-loader.ts` (389),
`services/quest-types.ts` (360), `core/recipe/recipe-editor.test.ts` (357),
`core/pack-health/checks/quests.ts` (324), `ProgressionGraph.tsx` (322),
`common/config-editor.tsx` (309). These are the next tier below the original top
~12; all are structurally sound, just long. The worst offender (`import.rs`) is
now split.
