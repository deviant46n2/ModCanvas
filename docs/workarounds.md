# Workaround Register — lived-experience facts that never made it into code

The s21 cont.4 lesson: the maintainer withheld "restart the app to refresh
textures" for weeks — a real, working workaround that died in one person's
head. This register exists so every such fact is surfaced at diagnostic
start, never hoarded. The `/workaround` command (and `/verify` step 6) enforces
consulting it.

| # | Workaround | When it applies | Why it works | Discovered |
|---|---|---|---|---|
| 1 | Restart the app to refresh stale icons/textures after engine or companion changes | Editor shows old icons after a backend/companion change | The app binary embeds the frontend AND backend; a stale process keeps serving the old cache (AGENTS.md rebuild rule) | s21 cont.4 (withheld for weeks — the lesson) |
| 2 | Read `debug.log`, not `latest.log`, for companion probes | Looking for probe output after a run | `latest.log` ROTATES AT MIDNIGHT — reading it alone faked a "no probes" read | s21 close |
| 3 | Verify a deployed jar actually RAN, not that it exists | After any companion rebuild/deploy | File existence ≠ new code: check md5 differs AND the new symbol is present (`javap -p -verbose`); `strings` won't show float constants | s14 |
| 4 | Kill a lingering game process before relaunch | Launch "fails" or the game doesn't come up | A stale game process holds the old jar in memory — mods load once at startup; the wrapper may be dead while the game lives | s21 cont.3 |
| 5 | Bump `engine_renders.rs` CACHE_VERSION in the SAME pass as a renderer-semantics change, then restart the app | Any companion change to shading/sampler/format semantics | The disk cache validates on version mismatch; an old binary keeps serving the stale cache | s14 / s21 |
| 6 | Purge the engine-render cache after a frontend-side render-guard fix — it will NOT self-heal | After changing which items the frontend queues to the engine (e.g. the s26 flat-icon guard: flats now never re-render) | The cache only regenerates items the frontend queues; unqueued items keep their stale renders forever, and bright materialized URLs never get persisted to the engine cache — the stale value re-flashes at every boot until the purge | 2026-08-09 (s27 → debt arc) |
| 7 | Prism "Could not download metadata for NeoForge X" — either **restart Prism** (real fix) or **switch the instance to a different NeoForge version** (quick fix) | A long-running Prism process (days) fails a loader launch with checksum mismatches even though the meta server is self-consistent | Prism's meta chain is hash-pinned: version file ← uid index ← root index, and the root index (root of trust) never re-downloads once loaded — the in-memory chain FREEZES at process start. Server regeneration after that → frozen expected hashes ≠ served content → permanent failure until the process dies. Restart re-fetches the now-consistent chain. Switching versions works only for versions whose files were NOT regenerated since process start. NOT the metacache (s37 diagnosis was wrong; deleting it changed nothing). | 2026-08-10 (s38, fully root-caused via PrismLauncher source + direct server probes) |
| 8 | App Launch button does nothing (silent) — **kill the stale prismlauncher process** and launch again | Clicked Launch, no error, no game window, nothing | A long-running `prismlauncher` process with an un-reaped zombie game process (`[java] <defunct>` under it) is wedged: its single-instance IPC accepts `--launch X` (CLI exits 0 immediately) but never starts the game. The app's `spawn_launch` nulls Prism's stdout/stderr (launcher.rs:178), so the refusal is invisible by construction. Tell: `pgrep -af prismlauncher` shows a launch from days ago + a java zombie; no fresh java spawns on relaunch. Kill the stale Prism (instances live on disk; nothing is lost) → fresh Prism launches normally. **Fixed in code (s44):** `do_launch` now watches a 20s grace window and fails with a specific "Prism exited immediately (code 0) and no game process started — close Prism and retry" message instead of reporting "Game exited (code 0)" as success. The tell still applies to manual diagnosis; the app now says it. | 2026-08-11 (s42, verified: CLI exited 0 with no process, then game booted after the kill) |
| 9 | `/ftbquests reload` does nothing — **enable FTB Quests commands + edit mode** (and use the slash) | Ran the reload command, nothing happens — no error, no reload | FTB Quests gates reload behind its editing permission: without "enable commands" + edit mode the command silently no-ops. Also: chat without the leading `/` is a chat message, not a command (log shows `[CHAT] ftbquests reload`, never a command dispatch). The companion's `sendCommand` prepends the slash, so the companion path is fine — this bites human probes. Evidence line for a REAL reload: `[Server thread/INFO] [FTB Quests/]: Loading quests from {folder}` (fires on world load too — timestamp-correlate, never whole-log grep). **Update (s43):** the companion now dispatches reloads through the integrated server's own command source (console op level 4, `server.createCommandSourceStack()`), so `hasEditorPermission`'s `hasPermission(2)` check passes — the app's hotswap path is no longer permission-gated. This workaround still applies to **human chat probes** and to fallback client-side dispatch (multiplayer/no integrated server). | 2026-08-11 (s42, instrument probe — two fires observed: world load 11:31:48, reload 12:14:44) |
| 10 | `kubejs reload` does nothing — on 1.21.1 it must be **`kubejs reload server-scripts`** (then `/reload` for datapack content) | Typed `/kubejs reload` in chat, nothing reloads (no error) | Bare `kubejs reload` has NO executor on 1.21.1 — the command tree is `reload config|startup-scripts|server-scripts` (verified against the shipped KubeJS 2101.7.2-build.368 jar, s44). And the script reload alone does NOT apply recipes/tags/loot tables — KubeJS's own command message says run `/reload` (vanilla datapack reload) for those. The app's hotswap path (s44) sends both commands and verifies both evidence lines; this bites human chat probes. Evidence lines in `latest.log`: `[KubeJS Server/]: Loaded N/N KubeJS server scripts in ...` (script reload) + `[KubeJS Server/]: Server resource reload complete!` (datapack apply) — both fire on world load too, pin-correlate, never whole-log grep. | 2026-08-11 (s44, jar bytecode + instance latest.log:304/311) |
| 11 | A template/first-open quest book shows 2 quests and no icons in-game while the editor shows all quests — **the template must use 1.21 Data Components item syntax, not bare strings**. **Fixed in code (s45):** template item tasks now use `item = { id = "minecraft:oak_log", count = 8 }`; fidelity lock `template_item_fields_are_never_bare_strings` fails on any bare `item = "..."`. | Template pack's first chapter collapses in-game after the first launch (2 stubs, no icons), editor still shows the full chapter | The template was hand-authored with the PRE-1.20.5 bare-string item form (`item = "minecraft:oak_log"`). On 1.21.1, FTB Quests 2101.1.30's `ItemTask.readData` → `itemOrMissingFromNBT` expects Data Components, `createTask` returns null, and `readQuestsFromNBT` calls `handleLegacyTaskNBT(null,…)` BEFORE its null-check → the task's `title` triggers `addTranslation(null)` → NPE → the whole book fails to load → the game auto-saves a stripped 2-quest chapter. The app's editor reads `.modcanvas/quests.json` (never involved) so it looks normal. Tell: game log `failed to load quest data … object is null` at `TranslationManager.makeKey`. Chapters with checkmark-only tasks survive. To restore a game-stripped instance: re-scaffold from the fixed template (backup the corrupted `quests/` first). | 2026-08-11 (s45, root-caused via game log NPE stack + FTB jar bytecode; fixed + locked) |
| 12 | A duplicate **unnamed tab** appears in-game after saving in the editor — a "ghost chapter" — while the editor shows one chapter. **Fixed in code (s56):** the flat export now prunes `chapters/*.snbt` whose chapter id is not in the graph (`cleanup_flat_layout`, locked by `export_removes_stale_flat_chapter_files_not_in_graph`). Until a fixed build is deployed: delete the stale file by hand (`rm config/ftbquests/quests/chapters/<lowercase_or_dup>.snbt`) and reload. | Game shows an extra tab (usually unnamed, often with working icons, no dependency lines) after an editor save + reload; the editor graph has only the "real" chapters | The exporter used to write every chapter the graph had, but its flat cleanup only removed stale SUBDIRS folders — orphaned flat chapter files whose chapter id later left the graph (a duplicated chapter deleted in the editor) were never deleted. The game loads EVERY file in `chapters/`, so the orphan loads as a second, duplicate book (13 quests = 6+6+1 shared acacia quest, matching the s56 observation). The stale file's quests carry the template's descriptions under fresh random ids with deps pointing at the OTHER file's quests — the signature of a duplicated chapter that was exported, then deleted from the graph. | 2026-08-14 (s56, mtime + log forensics; fixed + locked) |
| 13 | A scaffolded pack's quests show **no icons in-game** (but work after the first editor save). **Fixed in code (s56):** template `icon` fields now use the compound form `icon = { id = "minecraft:chest" }`; lock `template_icon_fields_are_never_bare_strings`. Until a fixed build is deployed: save once from the editor (the export rewrites icons as compounds). | Fresh wizard pack loads quests but quest icons are blank; the editor shows icons | Same class as #11 but for the ICON field: the template shipped bare-string `icon = "minecraft:chest"`. The game parses it but renders no icon; the app's exporter and the game's own rewrite both emit the compound form. The s45 item lock fixed `item`, never `icon`. | 2026-08-14 (s56, same-session as #12; template file vs exporter output diff) |
| 14 | After a host **nvidia driver update**, flatpak games die at launch with `GLX: Failed to find a suitable GLXFBConfig` — **run `flatpak update`** to refresh the `org.freedesktop.Platform.GL.nvidia-*` extension runtime, then relaunch. | Game window never appears; the app's launch reports a crash; the instance's `debug.log` shows EARLYDISPLAY trying GL 4.6→3.2, every attempt failing with the same GLXFBConfig error | The flatpak game renders through the sandbox's NVIDIA GL extension, which is version-locked to the driver it shipped with. After a host driver bump (e.g. 610.43.03 → 610.57.04), the sandbox's userspace GLX talks to the new kernel module and NVIDIA's version-locked ABI can't negotiate any framebuffer config. Host side is fine (`nvidia-smi` works, kernel module matches); only the extension is stale. No reboot required — the sandbox picks up the new runtime on next launch. | 2026-08-15 (s64, evidence: debug.log GLXFBConfig + flatpak list 610-43-03 vs nvidia-smi 610.57.04 + pacman.log driver bump; fix: flatpak update) |
| 15 | Plain `pnpm build` fails at the AppImage bundling step (`failed to run linuxdeploy`) — **use `NO_STRIP=1 pnpm build`** (the documented release invocation) | The release binary builds and the gate's stale-binary check passes, but `pnpm build` exits 1 at "Bundling … AppImage" | Tauri's AppImage bundler needs linuxdeploy; on this machine the strip step fails without the documented env (the roadmap's release path has always been `NO_STRIP=1 pnpm build` — s70–71 P0-DISTRIB). The release binary + deb + AppImage all build fine under `NO_STRIP=1`; the plain invocation is a red herring, not a regression. | 2026-08-17 (s73, verified both invocations same session) |

## Rules of the register

- Consult it at the start of every diagnostic; ask the student for their own
  lived-experience workarounds before proposing theories (they have some we
  don't — see #1).
- New workaround surfaced → add a row here with the date, in the same pass it
  is learned. Never let one die with a session.
- A workaround that gets fixed in code moves to "fixed" status with the commit
  hash — it stays as history, marked resolved.
